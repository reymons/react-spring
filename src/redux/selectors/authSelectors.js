export const isAuth = (state) => state.auth.isAuth;

export const getLogin = (state) => state.auth.login;

export const allowed = (state) => state.auth.allowed;

export const getUserId = (state) => state.auth.userId;